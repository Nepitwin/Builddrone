[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "builddrone-python-example"
version = "0.1.0"
description = "Small Python project built with Builddrone"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["main"]
